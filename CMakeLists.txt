# CMakeLists.txt
cmake_minimum_required(VERSION 3.10)  # Specify the CMake version

project(dinojump-fan-emulator-C C)  # Define the project name and specify it is a C project

# Add an executable target
add_executable(dinojump-fan-emulator-C src/main.c)
